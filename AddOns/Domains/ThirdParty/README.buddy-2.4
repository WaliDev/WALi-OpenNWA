The buddy-2.4 source included in this version is modified
only so that SCons can build buddy. This consists
of removing the use of config.h, and inlining the
macros in src/kernel.c. All changes are marked with // NAK.
There is a bug fix in src/reorder.c, also
marked with //NAK.

