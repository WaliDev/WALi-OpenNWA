# Build WALi library 
# $Id$

Import('BaseEnv')

xerces = SConscript('ThirdParty/SConscript')

parse  = SConscript('Source/SConscript', build_dir='_build/Parse',duplicate=0)

BaseEnv.Alias('xerces',xerces)

BaseEnv.Depends(parse,'xerces')

built = [xerces,parse]

Return('built')
