# Reachability example
import os

Import('WaliDir')
Import('Env')

ReachFiles=Split("""
    Reach.cpp
    ExReach.cpp
""")

ExReach = Env.Program('ExReach',ReachFiles)

i = Env.Install(os.path.join(WaliDir,'Examples','bin'), ExReach)

built = [i]

Return('built')

