detemplatize all classes?
dupTrans/dupTransOutgoing with regard to loops
addAllTrans performance
TransSet::isCall performance -- look in T_info
also naming of isCall between transinfo and transet
remove*TransWith performance
const by-value returns
remove*TransSym performance
transset::getInternalsFrom redundant; also, T_info.fromTrans

NWA::get_states and NWA::getStates, sizeStates/num_nwa_states, isState/is_nwa_state,
  sizeTrans/count_rules

check if plusWPDS calls realizeImplicitTransitions -- if not, maybe const
simulateWord
isMemberNondet: getReturns if we have a better index in transinfo?
                what to do on non-empty stack
nwa::clearSymbols() removes all transitions, even those on epsilon and wild
clearTrans and stuck



symbolset::clearSymbols -- add wild too
